import { DashBoardContainer } from "../containers/inventory/dashboard-container.js";

// Khởi tạo container khi trang được tải
document.addEventListener('DOMContentLoaded', () => {
        new DashBoardContainer();
});