import { ReportContainer } from "../containers/inventory/report-container.js";

// Khởi tạo container khi trang được tải
document.addEventListener('DOMContentLoaded', () => {
    new ReportContainer();
});